# applications_user

Place optional GrokLink-related FAPs here for `ufbt` / firmware `applications_user` builds.

Suggested:

- `groklink_status`  -  local UI for agent state / last audit hash
- Per-skill FAPs generated by skill craft (`fap_stub.c`)

Dynamic FAPs still live on SD under `/ext/apps/GrokLink/` after build.
