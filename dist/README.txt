GrokLink Firmware  -  local build artifacts (not tracked in git)

Place built firmware packages here after fbt, e.g.:
  momentum-f7-C/*.dfu

Flash with qFlipper (Install from file) or fbt flash_usb_full.

Copy sd_card/groklink from the repo root to the Flipper SD as /ext/groklink.
