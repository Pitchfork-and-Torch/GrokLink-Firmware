# Example skills

Bundled SD example: `sd_card/groklink/skills/lab_pulse_counter/`.

Craft more with:

```bash
groklink skill craft --log examples/sample_capture.jsonl --out skills/examples --name sample_crafted
```
